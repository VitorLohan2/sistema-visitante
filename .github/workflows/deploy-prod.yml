## TESTE
name: Deploy to Production

on:
  workflow_dispatch:  # Trigger manual pelo GitHub UI
  push:
    branches: [main]  # Trigger automático ao fazer merge na main

jobs:
  build-and-deploy:
    name: Build and Deploy to Production
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build da imagem de produção
        run: |
          echo "🏗️ Build da imagem de produção..."
          docker build -t ${{ secrets.DOCKER_USERNAME }}/liberae:prod ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/liberae:prod

      - name: Atualizar imagem latest
        run: |
          echo "🏷️ Atualizando imagem latest..."
          docker tag ${{ secrets.DOCKER_USERNAME }}/liberae:prod ${{ secrets.DOCKER_USERNAME }}/liberae:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/liberae:latest

  deploy-prod:
    name: Deploy to Production Server
    runs-on: self-hosted
    needs: build-and-deploy
    steps:
      - name: Deploy na produção
        working-directory: /home/dev/sistema/prod
        run: |
          echo "🔽 Parando containers de produção..."
          docker compose -f docker-compose-prod.yml down

          echo "⬇️ Baixando nova imagem de produção..."
          docker compose -f docker-compose-prod.yml pull

          echo "🚀 Subindo produção atualizada..."
          docker compose -f docker-compose-prod.yml up -d

          echo "🧹 Limpando imagens antigas..."
          docker image prune -f

          echo "✅ Deploy de produção concluído!"